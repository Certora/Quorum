# If you want to auto-activate the Poetry venv:
# load_poetry is a custom script or alias you define OR you do something like:
# layout python  # from direnv
# But better is to detect Poetry's local venv:
export VIRTUAL_ENV="$(poetry env info --path 2>/dev/null)"
if [[ -n "$VIRTUAL_ENV" ]]; then
  PATH_add "$VIRTUAL_ENV/bin"
fi
